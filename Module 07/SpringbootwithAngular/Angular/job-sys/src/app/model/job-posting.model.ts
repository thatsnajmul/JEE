export interface JobPosting {
    id: number;
    title: string;
    description: string;
    company: string;
    imageUrl: string; // URL to the image
    cvUrl: string;    // URL to the CV for download
  }
  